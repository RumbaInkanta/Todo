tasks (
        id UUID PRIMARY KEY,
        title TEXT,
        checked INTEGER,
        due_date TEXT,
        description TEXT,
        created_date TEXT,
	project_id UUID foreign key projects(id)
    )
projects (
	id UUID PRIMARY KEY,
        project_title TEX
    )